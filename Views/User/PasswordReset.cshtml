@model EvrakTakipSistemi.Models.ViewModels.ResetPasswordViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <title>Şifre Yenile</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
</head>
<body>
    <div class="container">
        <div class="modal fade" id="myModal" role="dialog">
            <div class="modal-dialog">
                <!-- Modal content-->
                <div class="modal-content">
                    <div class="modal-header">
                        <h4 class="modal-title" style="text-align:center;">YENİ ŞİFRE TALEP</h4>
                    </div>
                    <div class="modal-body">

                        @if (ViewBag.State == null)
                        {
                            @using (Html.BeginForm())
                            {
                                <label>Eposta:</label>
                                @Html.TextBoxFor(x => x.Email, new { @placeholder = "E-posta..", @class = "form-control" })

                                <div class="modal-footer">
                                    <button class="btn btn-primary" type="submit">Şifre Talep Et</button>
                                    <a href="/user/login" class="btn btn-danger">İptal</a>
                                </div>
                            }
                        }
                        else
                        {
                            if (ViewBag.State)
                            {
                                @:<div class="alert-info">Şifre talebi için e-posta adresinize bilgilerindirici mail gönderilmiştir.</div><br />
                                <div class="modal-footer">
                                    <a href="/user/login" class="btn btn-primary">Giriş Yap</a>
                                </div>

                            }
                            else
                            {
                                @:<div class="alert-warning">Böyle bir e-posta bulunmamaktadır.</div><br />
                                <div class="modal-footer">
                                    <a href="/user/passwordreset" class="btn btn-primary">Yeni Şifre Talep Et</a>
                                    <a href="/user/login" class="btn btn-danger">İptal</a>
                                </div>
                            }
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
<script>
    $(function () {
        $('#myModal').modal({ backdrop: 'static', keyboard: false, show: true });
    });
</script>